
# Portfolio Website Design

This is a Next.js (App Router) portfolio website.

## Running the code

1. Install dependencies:
   `pnpm install`
2. Start development server:
   `pnpm dev`
3. Lint:
   `pnpm lint`
4. Production build:
   `pnpm build`
  
